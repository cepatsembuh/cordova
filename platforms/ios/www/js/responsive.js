$(document).ready(function() {
  var width = $(window).width(),
      // costumizedWidth = width - 25,
      faskesImage = $('.faskes'),
      ads = $('.ads'),
      button = $('.faskes-btn');

  faskesImage.css('width', '100%')
  ads.css('width', '100%')
  button.css('width', '100%')
});
